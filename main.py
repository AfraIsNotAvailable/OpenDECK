import time
from core.AppController import AppController

# if __name__ == "__main__":
#     controller = AppController("Stremio", window_title_re=".*Stremio.*")

#     # Now you can call multiple actions without searching for the window each time.
#     controller.send_action("fullscreen")
#     time.sleep(1)
#     controller.send_action("library")
